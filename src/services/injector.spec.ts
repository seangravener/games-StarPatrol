describe("Given the Memory module", () => {
  beforeEach(() => {})

  it("is created", () => expect("st").toBe("st"))

  describe("and is loaded with expected results, then", () => {
    beforeEach(() => {})

    test("#recall() should return all memory", () => {})
  })
})
